rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Locked down by default: only signed-in users can read/write.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
